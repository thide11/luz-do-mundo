import 'package:flutter/material.dart';

class CreateEditActionPlan extends StatelessWidget {
  const CreateEditActionPlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}