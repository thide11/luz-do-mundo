import 'package:flutter/material.dart';

class ShowActionPlan extends StatelessWidget {
  const ShowActionPlan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}